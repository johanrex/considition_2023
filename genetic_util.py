def get_genome_length_from_location_names(location_names):
    return len(location_names * 2)


def update_solution(solution, location_names, individual):
    # order of location_names is assumed to have the same order every time.

    pass
